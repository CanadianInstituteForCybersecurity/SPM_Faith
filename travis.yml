language: python  # Specifies Python as the language

python:
  - "3.8"  # The version of Python you want to test against

# Install dependencies directly in the .travis.yml file
install:
  - pip install pytest  # Installs pytest directly

# Commands to run the tests
script:
  - pytest  # Runs pytest to execute the tests





















